const fs=require('fs'),path=require('path');const root=__dirname,source=path.join(root,'greatest-common-factor-problems.html'),route='/advanced-math/factoring/greatest-common-factor/problems/';if(!fs.existsSync(source))throw Error('Missing GCF problems source');const html=fs.readFileSync(source,'utf8');for(const m of ['Greatest Common Factor','SKILL CHECK','CORE PRACTICE','EXAM-STYLE PRACTICE','CHALLENGE PROBLEMS','3/8/5/2 required'])if(!html.includes(m))throw Error('GCF problems marker: '+m);const out=path.join(root,'dist',route.replace(/^\//,''));fs.mkdirSync(out,{recursive:true});fs.copyFileSync(source,path.join(out,'index.html'));const sp=path.join(root,'dist','schedule.json');if(fs.existsSync(sp)){let a=JSON.parse(fs.readFileSync(sp,'utf8')),b=a.filter(x=>x.route!==route),removed=a.length-b.length;if(removed>1)throw Error('Duplicate schedule route');if(removed){const step=20*60*1000,start=Math.ceil((Date.now()+60000)/step)*step,cairo=ms=>new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Cairo',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).format(new Date(ms)).replace(', ','T');b.forEach((x,i)=>{x.index=i+1;x.cairo=cairo(start+i*step)+'+03:00';x.timezone='Africa/Cairo'});fs.writeFileSync(sp,JSON.stringify(b,null,2));}}console.log('Built Greatest Common Factor problems route');