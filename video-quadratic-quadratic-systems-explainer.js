const fs=require('fs'),path=require('path');const d=path.join(__dirname,'dist','advanced-math','nonlinear-equations-and-functions','quadratic-quadratic-systems','video');fs.mkdirSync(d,{recursive:true});const html=`<!doctype html><html><head><meta charset="utf-8"><link rel="stylesheet" href="/styles.css"><title>Quadratic-Quadratic Systems — Video | SUMMIT MATH</title></head><body><main class="page-shell"><section class="lesson-card"><div class="page-type">FULL LESSON VIDEO</div><div class="lesson-title">Quadratic-Quadratic Systems</div><article class="lesson-content"><h2>Shared points solve both equations</h2><p>When both quadratics equal y, set their right sides equal. Every resulting real x-value can produce an intersection.</p><h2>Method</h2><p>Equate the quadratic expressions, simplify, solve the resulting linear or quadratic equation, then substitute back to find y and check each ordered pair.</p><h2>Example: cancellation</h2><p>y=x²-4x+5 and y=x²-2x+1. Cancel x²: -4x+5=-2x+1, so x=2 and y=1. The intersection is (2,1).</p><h2>Example: two intersections</h2><p>y=x² and y=-x²+8 gives 2x²=8, so x=±2 and y=4. The intersections are (-2,4) and (2,4).</p><h2>Exam strategy</h2><p>Compare coefficients before expanding. Matching x² terms cancel immediately. If x² remains, use factoring, square roots, or the quadratic formula and keep every real root.</p><h2>Consistency check</h2><p>A negative equation such as x²=-3 means no real intersections. A repeated root means one tangent intersection.</p><p><strong>This is the standard full lesson video route. Short-form social video is intentionally excluded.</strong></p></article></section></main></body></html>`;fs.writeFileSync(path.join(d,'index.html'),html);console.log('Quadratic-Quadratic Systems full lesson video route built; Shorts excluded.');