# Negative Slope — Retrofit Verification

- Route: `/algebra/linear-functions/negative-slope/explanation/`
- Classification: `BOTH`
- Graph added: yes (native deterministic graph already present in persistent lesson source)
- Desmos added: yes (lesson-specific `DESMOS STRATEGY` already present in persistent lesson source)
- Verified equation: `y = -2x + 3`
- Linear graph QA: `x=0 -> y=3`, `x=1 -> y=1`, `x=2 -> y=-1`; all three labeled points satisfy the equation by substitution.
- Slope cross-check: `(1-3)/(1-0) = -2`; from `(1,1)` to `(2,-1)`, `(-1-1)/(2-1) = -2`.
- Intercepts: y-intercept `(0,3)`; x-intercept is `(3/2,0)` from `0=-2x+3`.
- Desmos claim cross-check: the coefficient of `x` is `-2`, so the line decreases by 2 units in y for each 1-unit increase in x.
- Source teaching text preserved; no video/audio/player files touched.
- QA date: 2026-09-06.
