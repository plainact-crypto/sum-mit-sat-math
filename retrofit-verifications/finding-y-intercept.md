# Finding Y-Intercept — Retrofit Verification

- Route: `/algebra/linear-functions/finding-y-intercept/explanation/`
- Classification: `BOTH`
- Graph added/present: yes — native deterministic graph from `y = -3x + 7`.
- Desmos added/present: yes — lesson-specific `DESMOS STRATEGY` with Enter / Look for / Use it to answer / Why it works / Faster or not?.
- Linear graph QA: `x=0 -> y=7`, `x=1 -> y=4`, `x=2 -> y=1`; all three points satisfy `y = -3x + 7` by substitution.
- Y-intercept cross-check: setting `x=0` gives `y=7`, so the y-intercept is `(0,7)`.
- Slope cross-check: from `(0,7)` to `(1,4)`, `(4-7)/(1-0)=-3`, matching the coefficient of `x`.
- X-intercept check: `0=-3x+7` gives `x=7/3`.
- Desmos claim cross-check: the line crosses the y-axis at 7; reading `b=7` directly is faster when the equation is already in slope-intercept form.
- Existing teaching text preserved; no video/audio/player files touched.
- QA date: 2026-09-06.