const fs=require('fs'),path=require('path');
const root=__dirname,dist=path.join(root,'dist');
const sourceName='solving-by-square-roots-test.html';
const route='/advanced-math/quadratic-equations/solving-by-square-roots/test/';
const source=path.join(root,sourceName);
if(!fs.existsSync(source))throw new Error(`Missing ${sourceName}`);
const html=fs.readFileSync(source,'utf8');
for(const marker of ['LESSON TEST','Solving by Square Roots','Results and explanations appear only after Submit Test.','HARD / EXAM-STYLE'])if(!html.includes(marker))throw new Error(`${sourceName} failed marker: ${marker}`);
if((html.match(/level:'/g)||[]).length!==5)throw new Error('Lesson Test must contain exactly 5 questions');
const target=path.join(dist,route.replace(/^\//,''));fs.mkdirSync(target,{recursive:true});fs.copyFileSync(source,path.join(target,'index.html'));
console.log('Built Solving by Square Roots lesson test route');