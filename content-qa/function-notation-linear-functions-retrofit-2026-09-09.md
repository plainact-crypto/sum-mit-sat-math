# Function Notation for Linear Functions — Explanation Retrofit QA

- Route: `/algebra/linear-functions/function-notation-linear-functions/explanation/`
- Classification: `BOTH`
- Graph added/present: yes — deterministic native graph from `f(x)=2x+5`
- Desmos added/present: yes — lesson-specific `DESMOS STRATEGY` with Enter / Look for / Use it to answer / Why it works / Faster or not?
- Source teaching text preserved.

## Independent math cross-check

For `f(x)=2x+5`:
- `f(0)=2(0)+5=5`, so `(0,5)` is on the line.
- `f(1)=2(1)+5=7`, so `(1,7)` is on the line.
- `f(3)=2(3)+5=11`, so `(3,11)` is on the line.
- The y-intercept is `(0,5)`.
- The x-intercept solves `0=2x+5`, hence `x=-5/2=-2.5`.
- The lesson's negative-input example is correct: `f(-4)=2(-4)+5=-3`.
- The context example is correct: `C(6)=2.5(6)+4=19` dollars.

The graph's labeled points, intercept claims, and Desmos result are therefore independently verified without relying on the rendered graph or Desmos output.