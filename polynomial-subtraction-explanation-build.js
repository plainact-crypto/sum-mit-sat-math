const fs=require('fs'),path=require('path');
const root=__dirname,dist=path.join(root,'dist'),source=path.join(root,'polynomial-subtraction-explanation.html');
if(!fs.existsSync(source))throw new Error('Missing polynomial-subtraction-explanation.html');
const route='/advanced-math/polynomial-operations/polynomial-subtraction/explanation/';
const html=fs.readFileSync(source,'utf8');
for(const marker of ['<div class="page-type">Explanation</div>','<div class="lesson-title">Polynomial Subtraction</div>','lesson-content','EXPLANATION_CLASSIFICATION: NEITHER','3x² + 10x - 10','3a³ - 3a² + 4a + 6','4x² - 4x + 5'])if(!html.includes(marker))throw new Error(`Explanation source failed marker: ${marker}`);
if(/summit-desmos-strategy|data-summit-graph/i.test(html))throw new Error('NEITHER Explanation must not include Graph or Desmos blocks');
const target=path.join(dist,route.replace(/^\//,''));fs.mkdirSync(target,{recursive:true});fs.copyFileSync(source,path.join(target,'index.html'));
const problemsSource=path.join(root,'polynomial-subtraction-problems.html'),problemsRoute='/advanced-math/polynomial-operations/polynomial-subtraction/problems/';
if(fs.existsSync(problemsSource)){const ph=fs.readFileSync(problemsSource,'utf8');for(const marker of ['PRACTICE PROBLEMS','Polynomial Subtraction','18 questions','SKILL CHECK','CORE PRACTICE','EXAM-STYLE PRACTICE','CHALLENGE PROBLEMS'])if(!ph.includes(marker))throw new Error(`Problems source failed marker: ${marker}`);const pt=path.join(dist,problemsRoute.replace(/^\//,''));fs.mkdirSync(pt,{recursive:true});fs.copyFileSync(problemsSource,path.join(pt,'index.html'));}
const sp=path.join(dist,'schedule.json');
function cairo(ms){return new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Cairo',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).format(new Date(ms)).replace(', ','T')}
if(fs.existsSync(sp)){const original=JSON.parse(fs.readFileSync(sp,'utf8')),completed=new Set([route]);if(fs.existsSync(problemsSource))completed.add(problemsRoute);const s=original.filter(r=>!completed.has(r.route)),removed=original.length-s.length,interval=20*60*1000,start=Math.ceil((Date.now()+60000)/interval)*interval;if(removed>2)throw new Error(`Expected at most two scheduled Polynomial Subtraction routes; removed ${removed}`);if(removed>0){s.forEach((r,i)=>{r.index=i+1;r.cairo=cairo(start+i*interval)+'+03:00';r.timezone='Africa/Cairo'});fs.writeFileSync(sp,JSON.stringify(s,null,2));const q=v=>'"'+String(v??'').replace(/"/g,'""')+'"';fs.writeFileSync(path.join(dist,'schedule.csv'),'index,subject,section,group,lesson,pageType,route,cairo,timezone\n'+s.map(r=>[r.index,r.subject,r.section,r.group,r.lesson,r.pageType,r.route,r.cairo,r.timezone].map(q).join(',')).join('\n')+'\n');const ap=path.join(dist,'audit.json');if(fs.existsSync(ap)){const a=JSON.parse(fs.readFileSync(ap,'utf8'));a.completedContentPages=(a.completedContentPages||0)+removed;a.scheduledPages=s.length;a.contentSlotMinutes=20;fs.writeFileSync(ap,JSON.stringify(a,null,2));}}}
