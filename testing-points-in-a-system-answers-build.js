const fs=require('fs'),path=require('path');const root=__dirname,dist=path.join(root,'dist'),route='algebra/systems-of-linear-inequalities/testing-points-in-a-system/answers/',source=path.join(root,'testing-points-in-a-system-answers.html');if(!fs.existsSync(source))throw new Error('Missing testing points answers source');const target=path.join(dist,route);fs.mkdirSync(target,{recursive:true});fs.copyFileSync(source,path.join(target,'index.html'));const sp=path.join(dist,'schedule.json');if(fs.existsSync(sp)){let s=JSON.parse(fs.readFileSync(sp,'utf8')).filter(r=>r.route!==`/${route}`);const interval=20*60*1000,start=Math.ceil((Date.now()+60000)/interval)*interval;function p(ms){const d=new Date(ms),iso=new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Cairo',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).format(d).replace(', ','T');return iso} s.forEach((r,i)=>{r.index=i+1;r.cairo=`${p(start+i*interval)}+03:00`;r.timezone='Africa/Cairo'});fs.writeFileSync(sp,JSON.stringify(s,null,2));const csv='index,subject,section,group,lesson,pageType,route,cairo,timezone\n'+s.map(r=>[r.index,r.subject,r.section,r.group,r.lesson,r.pageType,r.route,r.cairo,r.timezone].map(v=>`"${String(v??'').replace(/"/g,'""')}"`).join(',')).join('\n')+'\n';fs.writeFileSync(path.join(dist,'schedule.csv'),csv);const ap=path.join(dist,'audit.json');if(fs.existsSync(ap)){const a=JSON.parse(fs.readFileSync(ap,'utf8'));a.completedContentPages=(a.completedContentPages||0)+1;a.scheduledPages=s.length;a.contentSlotMinutes=20;fs.writeFileSync(ap,JSON.stringify(a,null,2))}}