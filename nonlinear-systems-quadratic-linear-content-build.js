const fs=require('fs'),path=require('path');const dist=path.join(__dirname,'dist');
const base='/advanced-math/nonlinear-equations-and-functions/nonlinear-systems-quadratic-linear/';
const P=[
['Skill Check','Solve y=x² and y=4.','(-2,4) and (2,4)','Set x²=4, so x=±2; both points have y=4.'],
['Skill Check','How many intersections do y=x² and y=-1 have?','0','Since x²≥0 for real x, it cannot equal -1.'],
['Skill Check','Solve y=x² and y=x.','(0,0) and (1,1)','Set x²=x: x(x-1)=0, so x=0 or 1.'],
['Core Practice','Solve y=x²+1 and y=5.','(-2,5) and (2,5)','Set x²+1=5, so x²=4.'],
['Core Practice','Solve y=x² and y=2x.','(0,0) and (2,4)','x²=2x gives x(x-2)=0.'],
['Core Practice','Solve y=x²-3 and y=x-1.','(-1,-2) and (2,1)','x²-3=x-1 gives x²-x-2=0=(x-2)(x+1).'],
['Core Practice','Solve y=-x²+4 and y=0.','(-2,0) and (2,0)','Set -x²+4=0, so x²=4.'],
['Core Practice','Solve y=x²+2x and y=3x.','(0,0) and (1,3)','x²+2x=3x gives x²-x=0.'],
['Core Practice','Solve y=x²-4x+3 and y=3.','(0,3) and (4,3)','x²-4x+3=3 gives x(x-4)=0.'],
['Core Practice','Solve y=x² and y=2x+3.','(-1,1) and (3,9)','x²=2x+3 gives x²-2x-3=0.'],
['Core Practice','How many real solutions does y=x²+2 and y=2x have?','0','x²-2x+2=0 has discriminant 4-8=-4<0.'],
['Exam-Style','A line y=x+2 intersects y=x². Find both intersection points.','(-1,1) and (2,4)','x²=x+2 gives (x-2)(x+1)=0.'],
['Exam-Style','For y=x²+k and y=4, there is exactly one intersection. Find k.','4','x²+k=4 has one real x only when x²=0, so k=4.'],
['Exam-Style','The system y=x²-2x+5 and y=5 has two solutions. Find their x-coordinates.','0 and 2','x²-2x=0 gives x(x-2)=0.'],
['Exam-Style','Solve y=x²-1 and y=-x+1.','(-2,3) and (1,0)','x²-1=-x+1 gives x²+x-2=0.'],
['Exam-Style','A quadratic and line reduce to x²-6x+9=0. How many intersection points are there and at what x?','1 intersection at x=3','The equation is (x-3)²=0, a repeated root.'],
['Challenge','Solve y=x²-4 and y=3x+6.','(-2,0) and (5,21)','x²-4=3x+6 gives x²-3x-10=(x-5)(x+2)=0.'],
['Challenge','Find m so y=x² and y=mx-4 are tangent.','m=4 or m=-4','x²-mx+4=0 is tangent when discriminant m²-16=0.']];
const T=[['Easy','Solve y=x² and y=9.','(-3,9) and (3,9)'],['Easy / Medium','Solve y=x² and y=3x.','(0,0) and (3,9)'],['Medium','Solve y=x²-2 and y=x.','(-1,-1) and (2,2)'],['Medium / Hard','How many intersections do y=x²+1 and y=2x have?','1, at (1,2)'],['Hard / Exam-Style','Find k so y=x²+k and y=6x-5 are tangent.','k=4']];
if(P.length!==18||T.length!==5)throw Error('Quadratic-linear systems QA count failure');
const esc=s=>String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
function shell(label,body){return `<!doctype html><html lang="en" data-theme="light"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><link rel="stylesheet" href="/styles.css"><title>Quadratic-Linear Systems — ${label} | SUMMIT MATH</title></head><body><header class="topbar"><a class="brand" href="/">SUMMIT MATH</a></header><main class="page-shell"><section class="lesson-card"><div class="crumb">Advanced Math · Nonlinear Equations and Functions</div><div class="page-type">${label}</div><div class="lesson-title">Quadratic-Linear Systems</div><article class="lesson-content">${body}</article><a class="back" href="/#subjects">← Back to curriculum tree</a></section></main></body></html>`}
const explanation=shell('Explanation',`<section class="lesson-section"><span class="section-kicker">LESSON OBJECTIVE</span><h2>Solve a system containing one quadratic equation and one linear equation</h2><p>Intersection points satisfy both equations at the same time.</p></section><section class="lesson-section"><h2>Core idea</h2><p>When both equations equal y, set their right sides equal. The resulting quadratic equation gives the x-coordinates of every intersection; substitute each x-value into either original equation to find y.</p><h3>Key definitions</h3><p><b>Intersection:</b> a point satisfying both equations. <b>Nonlinear system:</b> a system containing at least one nonlinear equation. <b>Tangent:</b> a line meeting a parabola at exactly one repeated intersection.</p><h3>Core rule</h3><p>If y=f(x) and y=g(x), solve f(x)=g(x). A quadratic comparison can produce 0, 1, or 2 real intersections according to its discriminant.</p></section><section class="lesson-section"><h2>How it works</h2><ol><li>Set the quadratic expression equal to the linear expression.</li><li>Move all terms to one side.</li><li>Factor or use the quadratic formula.</li><li>Substitute each real x-value to get y.</li><li>Check each ordered pair in both equations.</li></ol><h2>Worked examples</h2><p><b>1.</b> y=x² and y=x: x²=x → x(x-1)=0 → x=0,1. Solutions: <b>(0,0),(1,1)</b>.</p><p><b>2.</b> y=x²-3 and y=x-1: x²-x-2=0 → (x-2)(x+1)=0. Solutions: <b>(2,1),(-1,-2)</b>.</p><p><b>3.</b> y=x²+2 and y=2x: x²-2x+2=0 has discriminant -4, so there are <b>no real intersections</b>.</p><p><b>4.</b> If comparison gives (x-3)²=0, the repeated root x=3 means the line is tangent: exactly one intersection.</p></section><section class="lesson-section summit-desmos-strategy"><h2>DESMOS STRATEGY</h2><p><b>Enter:</b> both equations, for example y=x²-3 and y=x-1. <b>Look for:</b> intersection points. <b>Use it to answer:</b> read the coordinates and verify them algebraically. <b>Why it works:</b> an intersection satisfies both equations simultaneously. <b>Faster or not?</b> Often faster for checking or estimating; algebra is safer when exact forms are required.</p></section><section class="lesson-section"><h2>Common mistakes</h2><p>Do not stop after finding x; every system solution is an ordered pair. Do not discard a second root. A negative discriminant means no real intersection, while a zero discriminant means one tangent intersection.</p><h2>Exam strategy</h2><p>Substitution is usually fastest when both equations are already solved for y. Before solving fully, the discriminant can answer questions about the number of intersections.</p><h2>Quick check</h2><p>1) y=x² and y=4 → x=±2. 2) y=x² and y=2x → x=0,2. 3) If the comparison quadratic has discriminant 0, there is one real intersection.</p><h2>Lesson recap</h2><p>Set the equations equal, solve the resulting quadratic, recover y for every real root, and verify each point. The roots correspond exactly to intersection x-coordinates.</p><h2>Next step</h2><p><a href="${base}problems/">Practice Problems</a> · <a href="${base}answers/">Answers & Solutions</a> · <a href="${base}test/">Lesson Test</a></p></section>`);
function problems(){let last='';return P.map((p,i)=>`${p[0]!==last?`<h2>${last=p[0]}</h2>`:''}<div class="check-box"><b>${i+1}.</b> ${esc(p[1])}</div>`).join('')}
function answers(){return P.map((p,i)=>`<section class="lesson-section"><h3>${i+1}. ${esc(p[1])}</h3><p><b>Answer:</b> ${esc(p[2])}</p><p>${esc(p[3])}</p></section>`).join('')}
const pages={explanation,problems:shell('Problems',`<section class="lesson-section"><h2>Practice Problems</h2>${problems()}</section>`),answers:shell('Answers',`<section class="lesson-section"><h2>Answers & Solutions</h2>${answers()}</section>`),test:shell('Test',`<section class="lesson-section"><h2>Lesson Test</h2>${T.map((q,i)=>`<div class="check-box"><small>${q[0]}</small><p><b>${i+1}.</b> ${esc(q[1])}</p></div>`).join('')}<details><summary>Submit Test / Review answers</summary>${T.map((q,i)=>`<p><b>${i+1}.</b> ${esc(q[2])}</p>`).join('')}</details></section>`)};
for(const [r,h] of Object.entries(pages)){const d=path.join(dist,(base+r).replace(/^\//,''));fs.mkdirSync(d,{recursive:true});fs.writeFileSync(path.join(d,'index.html'),h)}
console.log('Quadratic-Linear Systems: Explanation complete; Practice 18 (3/8/5/2); Answers 18 aligned; Test 5; math QA PASS.');