const fs=require('fs'),path=require('path');const src=path.join(__dirname,'polynomial-division-answers.html');const route='/advanced-math/polynomial-operations/polynomial-division/answers/';const out=path.join(__dirname,'dist',route.replace(/^\//,''),'index.html');if(!fs.existsSync(src))throw new Error('Missing Polynomial Division answers source');const html=fs.readFileSync(src,'utf8');if((html.match(/class="answer"/g)||[]).length!==18||!html.includes('Solutions correspond exactly to Practice Questions 1–18.'))throw new Error('Polynomial Division answers must contain exactly 18 solutions');fs.mkdirSync(path.dirname(out),{recursive:true});fs.copyFileSync(src,out);const built=fs.readFileSync(out,'utf8');if(!built.includes('Polynomial Division')||!built.includes('Answer: A) x² - 4')||!built.includes('Answer: A) -1'))throw new Error('Polynomial Division answers route QA failed');const sp=path.join(__dirname,'dist','schedule.json');function cairo(ms){return new Intl.DateTimeFormat('en-CA',{timeZone:'Africa/Cairo',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).format(new Date(ms)).replace(', ','T')}if(fs.existsSync(sp)){const original=JSON.parse(fs.readFileSync(sp,'utf8')),s=original.filter(r=>r.route!==route),removed=original.length-s.length,interval=20*60*1000,start=Math.ceil((Date.now()+60000)/interval)*interval;if(removed>1)throw new Error(`Expected at most one scheduled Polynomial Division answers route; removed ${removed}`);if(removed){s.forEach((r,i)=>{r.index=i+1;r.cairo=cairo(start+i*interval)+'+03:00';r.timezone='Africa/Cairo'});fs.writeFileSync(sp,JSON.stringify(s,null,2));}}console.log('Built Polynomial Division answers route');