const fs=require('fs');const path=require('path');const root=__dirname;const base=path.join(root,'dist','algebra','linear-equations-in-one-variable','solving-one-step-linear-equations');const copies=[['problems','one-step-problems.html'],['answers','one-step-answers.html']];for(const [dir,file] of copies){const target=path.join(base,dir);fs.mkdirSync(target,{recursive:true});fs.copyFileSync(path.join(root,file),path.join(target,'index.html'));}